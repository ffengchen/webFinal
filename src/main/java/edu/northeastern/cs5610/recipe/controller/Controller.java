package edu.northeastern.cs5610.recipe.controller;

/**
 * The interface Controller.
 */
public interface Controller {

  /**
   * Initialize the controller and whatever resources it needs. Connect to databases, set up default
   * values, etc.
   */
  void register();
}
